const allSection=document.querySelectorAll('.container >section');
const preBtn=document.querySelector('.arrow-wrap .pre');
const nextBtn=document.querySelector('.arrow-wrap .next');
console.log(allSection);